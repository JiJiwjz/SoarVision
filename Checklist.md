# 翱翔智眸 · 冲国特施工清单

> 目标:国特(获奖率 ~1%)。当前状态 = **强国奖底座已就位**,缺三块差异化。
> 判据:作品排名 = 客观测评排名 + 线下评审排名 之和(越小越好)。
> 图例:`[x]` 完成 / `[~]` 进行中 / `[ ]` 待办 ‖ 每项带**验收标准**(达到才算"做到")。

---

## 0. 已就位的底座(地基,约占工作量 60%)

- [x] RF-DETR 主干 + YOLO26n 基线(检测器选型已领先 2024 国特的 DETR+FA)
- [x] hi-res@896 训练(小目标 recall 2%→15.7%)
- [x] **D1 退化鲁棒训练**:浓雾 mAP +5.5(small)/+7.0(nano),晴天不掉,抗雾稳定性 ×5(论文背书齐全)
- [x] 评测体系:分级退化集 + 逐条件鲁棒表 + 对比图 + 定性图
- [x] **X1 可部署性**:ONNX 导出 + TRT 可转换性验证(无不支持算子)
- [x] git commit 全程留痕(规则明确作依据)

> ⚠ 但这些是"把已知方法执行得很扎实"。国特要的是**针对场景核心难点(雾里远小目标)的、新颖且验证过的贡献**——即下面三块。

---

## 🅰 多模态雷达融合 ★最高优先 · 最大差异化★

> 赛题创新轴第一项就是"多模态融合",场景立论就是"光学失效→上雷达"。**这是国特的命门。**

- [x] **A1 · WaterScenes 数据落地**:解压各组件 zip 到 `datasets/WaterScenes/`,跑 `radar/waterscenes.py --validate`。✅ 本地校验通过(7类/划分 train37884·val10824·test5412);**雷达点对齐确认**(frame 29543 雷达点精准落在远岸货船上,无系统偏移)。修复:`list_frames` 用 `Path().stem` 兼容 `./images/<id>.jpg` 路径格式。
- [x] **A2 · REVP 归一化标定**:据真实直方图(500帧)调 `RevpNorm` — doppler_abs 30→12(数据±11)、elev_abs 30→22、range_max=200✓、power per-frame。每帧中位 303 点。
- [~] **A3 · RF-DETR decoder 雷达 cross-attention(RGQ)**:REVP 特征作 K/V,object queries 软关联(TransCAR 路线),留置信门控接口。
  - 积木 ✅(`radar/fusion.py` gate=0=恒等)。**集成 ✅**(`radar/integrate.py`:0.58M 参数接进 nano 2 层 decoder,forward_post class-patch 带守卫,**雷达关闭时预测与 baseline 字节级一致=补丁无害**)。剩:radar-ON 端到端前向 + 训练收敛(=A4,rfdetr.train 喂不了 REVP,需自定义训练循环)。
  - **验收**:前向/反向不崩,能在 WaterScenes 上训练收敛。
  - **纪律**:加完跑 X1 导出冒烟(`rfdetr_export.py` + `trt_build_benchmark.py`)确认仍可转 ONNX/TRT。
- [~] **A4 · 多模态消融表(答辩核心)**:WaterScenes 上 RGB-only vs RGB+radar,逐条件(晴/夜/雨)对比。
  - 进度:**训练步已验证**(`radar/train_fusion.py --smoke`:复用 RF-DETR criterion/matcher,forward+loss+反传,25 雷达参数有梯度,loss=14.97)。剩:真实 WaterScenes dataloader(接 `radar/dataset.py`)+ epoch 循环 + 解压 WaterScenes + 跑 RGB vs RGB+radar 训练对比(需 GPU)。
  - **验收(硬指标)**:**融合在恶劣条件(夜/雨)上 漏检↓ ≥5% 或 mAP@50:95 ↑ ≥2 个点,且 clear 不显著掉。** 达到即"多模态真的有用"成立。
- [ ] **A5 · 雷达稀疏化增强**:训练时随机下采样 WaterScenes 点,模拟 IWR6843 稀疏。
  - **验收**:在稀疏化测试集上 mAP 掉幅明显小于无增强(证明对我们真雷达的稀疏鲁棒)。
- [ ] **A6 · 自采 IWR6843 配对数据微调 + demo**(后期,与物理 demo 合并)。
  - **验收**:自采片段上融合模型能出检测,且雷达起到作用(雾/夜下补漏)。

---

## 🅱 落地且验证的新结构 ★算法新颖性★

> D1 本质是"已知的联合退化增强"。国特需要至少一个**自己的、消融验证过的结构创新**。选 1–2 落地。

- [ ] **B1 · 细尺度小目标头/特征**:攻"雾里小目标 recall≈0"的核心弱点。
  - **验收**:**clear 小目标 recall 12%→≥25%;fog_medium/heavy 下不再 ≈0(>5%)。** 带参数量/FPS 代价报告。
- [ ] **B2 · DA-FP 退化自适应物理-频率模块**:估退化度→自适应增强(含卷积版 Haar 小波),接在 D1 之上。
  - **验收**:退化子集上比纯 D1 再涨(目标 fog_heavy +1~2 mAP),clear 不掉;X1 可导出(Haar 用卷积实现,别用原生 DWT)。
- [ ] (RGQ 见 A3,本身即一项结构创新)
- **纪律**:每个新结构 = 一次累加消融行 + 一次 X1 导出冒烟。

---

## 🅲 真实部署 + 物理验证 ★方案成熟度/实时性★

- [ ] **C1 · Jetson 实时性**:Orin Nano 上 `trtexec --onnx=rfdetr-nano.onnx --fp16` 转 TRT,测 FPS/延迟/显存。
  - **验收**:**nano 在 Orin Nano 上 ≥15–20 FPS @ 部署分辨率**(第 5 指标"实时性"的真值)。
- [ ] **C2 · 真实水域物理 demo**:湖/水库/码头,RC 船 + 浮标摆 20–200m,传感器装机器人/三脚架。
  - **验收**:一段真实采集视频 + **"准确率 vs 距离"曲线**(答辩杀手图;泳池不算,见 `docs/evaluation-plan.md`)。
- [ ] **C3 · VRX 仿真雾(可选)**:出 "mAP vs 能见度" 曲线,补物理造不出的浓雾。
  - **验收**:能见度分级曲线图,对应琼州海峡 <500m 场景。

---

## 🅳 收口 · 答辩材料(并行/最后)

- [ ] 三张赛题要求的表:**鲁棒性曲线 ✅ / 多模态消融(A4)/ 实时性(C1)**
- [ ] 定性图集(已有晴天 + 退化 GT|baseline|D1;补融合前后、物理 demo)
- [ ] 方案说明(含 git commit 记录)+ 路演 PPT + vlog(≤6min,≤3G)
- [ ] 全程 git 留痕

---

## 施工顺序(按依赖 + 差异化)

| 阶段 | 内容 | 为什么 |
|---|---|---|
| **P1（现在）** | A1→A2→A3→**A4** | 雷达融合跑通 + 消融表 = 国特命门 |
| **P2（并行）** | **B1** 小目标头 | 攻核心技术弱点(雾里小目标) |
| **P3** | C1 Jetson 实时性 | 补第 5 指标真值 |
| **P4** | B2 / A5 / C2 物理 demo | 加深创新 + 成熟度 |
| **P5** | A6 自采微调 + D 收口 | 答辩材料定稿 |

> A1/A2 **不需要 GPU**(纯解析/可视化),本机即可开干;A3/A4 训练需 GPU。

---

## ✅ "踏进国特门槛"的总判据

同时满足这三条,才算从"强国奖底座"跨进国特区间:

1. **A4 ✓** — 多模态消融表证明融合在恶劣条件下确实降漏检(硬数字)。
2. **B1 或 B2 ✓** — 至少一个自己的新结构,消融证明有效。
3. **C1 + C2 ✓** — Jetson 实时性达标 + 一次可信的真实 demo。

> 好消息:这三件的数据/思路/接口都已铺好(WaterScenes 已传、融合走 query 级软关联、RGQ/DA-FP 设计已定),是**按图施工**而非从零探索。
